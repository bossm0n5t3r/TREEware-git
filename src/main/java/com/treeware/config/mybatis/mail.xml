<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.treeware.email.dao.EmailDao">

	<insert id="sendMail" parameterType="MailDto" >
	insert into ML(ML_SQ, ML_GRP_SQ, ML_STT_SQ, EMP_SQ, ML_SND_ADD, ML_RCV_ADD, ML_TTL, ML_CTT) 
	values(#{ml_sq}, 2, 2, #{emp_sq}, #{ml_snd_add}, #{ml_rcv_add}, #{ml_ttl}, #{ml_ctt})
	</insert>
	
	<select id="getNextSeq" resultType="int">
	select ML_SQ.nextval
	from dual
	</select>
	
</mapper>

